{
  "name": "traffic-rules",
  "version": "3.0.0",
  "private": true,
  "description": "Desktop app for managing iptables on remote Linux servers via SSH",
  "scripts": {
    "dev": "tauri dev",
    "dev:web": "vite --port 1420",
    "build": "tauri build",
    "tauri": "tauri",
    "check": "tsc --noEmit",
    "test": "cargo test --manifest-path src-tauri/Cargo.toml",
    "test:all": "npm run check && npm run test",
    "lint": "tsc --noEmit"
  },
  "author": "lifeart",
  "license": "MIT",
  "devDependencies": {
    "@tauri-apps/cli": "^2.10.1",
    "@types/sortablejs": "^1.15.9",
    "esbuild": "^0.27.4",
    "typescript": "^5.9.3",
    "vite": "^8.0.1"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.10.1",
    "@tauri-apps/plugin-updater": "^2",
    "idb": "^8.0.3",
    "sortablejs": "^1.15.7"
  }
}
